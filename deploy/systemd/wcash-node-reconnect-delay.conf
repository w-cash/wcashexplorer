[Service]
# The public Testnet seed deliberately rejects repeated connections from the
# same IP for 119 seconds. Avoid turning a transient node restart into a
# permanently failed single-seed bootstrap attempt.
RestartSec=125s
